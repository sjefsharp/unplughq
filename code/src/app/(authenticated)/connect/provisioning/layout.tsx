import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Provisioning (Step 3 of 3) — UnplugHQ",
};

export default function ProvisioningLayout({ children }: { children: React.ReactNode }) {
  return children;
}
