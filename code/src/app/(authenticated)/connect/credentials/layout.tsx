import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Connect Server (Step 1 of 3) — UnplugHQ",
};

export default function CredentialsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
