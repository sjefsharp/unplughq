# unplughq — Project Instructions

This project uses the Agentic Framework for multi-agent software delivery.

## Project Rules

1. All artifacts are stored in `artifacts/` under work-item directories.
2. Source code lives in `code/`.
3. Reports and telemetry are stored in `reports/`.
